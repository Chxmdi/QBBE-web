"use client";
import Link from "next/link";
import { useState } from "react";
import { programs, events } from "@/lib/content";
import { localePath, type Locale } from "@/lib/i18n";
export default function SearchPage({ params }: { params: Promise<{ locale: Locale }> }) { const [query, setQuery] = useState(""); const [locale, setLocale] = useState<Locale>("en"); void params.then(({ locale: value }) => setLocale(value)); const needle = query.toLowerCase(); const results = [...programs.map((item) => ({ type: locale === "en" ? "Program" : "Programme", title: item.title[locale], href: `/programs/${item.slug}`, description: item.summary[locale] })), ...events.map((item) => ({ type: locale === "en" ? "Event" : "Événement", title: item.title[locale], href: `/events/${item.slug}`, description: item.description[locale] }))].filter((item) => !needle || `${item.title} ${item.description}`.toLowerCase().includes(needle)); return <><section className="page-hero"><div className="shell"><p className="eyebrow">{locale === "en" ? "Search" : "Recherche"}</p><h1 className="display">{locale === "en" ? "Find what you need." : "Trouvez ce qu’il vous faut."}</h1></div></section><section className="section"><div className="shell" style={{ maxWidth: 800 }}><label className="field"><span>{locale === "en" ? "Search QBBE" : "Rechercher sur QBBE"}</span><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} placeholder={locale === "en" ? "Programs, events, resources…" : "Programmes, événements, ressources…"} /></label><div className="search-results" aria-live="polite">{results.map((result) => <Link key={result.href} className="search-result" href={localePath(locale, result.href)}><p className="eyebrow" style={{ color: "var(--blue)" }}>{result.type}</p><h2 className="display" style={{ fontSize: "2rem", margin: ".25rem 0" }}>{result.title}</h2><p>{result.description}</p></Link>)}{query && !results.length && <p>{locale === "en" ? "No results found." : "Aucun résultat trouvé."}</p>}</div></div></section></>; }
