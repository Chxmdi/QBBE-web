"use client";
import { useState } from "react";

export function ContactForm({ locale, type = "contact" }: { locale: "en" | "fr"; type?: "contact" | "volunteer" | "partner" }) { const [state, setState] = useState<"idle" | "sending" | "sent" | "error">("idle"); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setState("sending"); const form = new FormData(event.currentTarget); const response = await fetch("/api/contact", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ name: form.get("name"), email: form.get("email"), subject: form.get("subject"), message: form.get("message"), consent: form.get("consent"), type, locale }) }); setState(response.ok ? "sent" : "error"); } if (state === "sent") return <div className="form-shell"><h2 className="display" style={{ fontSize: "2.7rem" }}>{locale === "en" ? "Thank you. We’ll be in touch." : "Merci. Nous vous contacterons bientôt."}</h2></div>; return <form className="form-shell" onSubmit={submit}><p className="eyebrow" style={{ color: "var(--blue)" }}>{type === "volunteer" ? (locale === "en" ? "Volunteer with QBBE" : "Bénévolat avec QBBE") : type === "partner" ? (locale === "en" ? "Partner with QBBE" : "Devenez partenaire de QBBE") : (locale === "en" ? "Contact QBBE" : "Contacter QBBE")}</p><div className="form-row"><div className="field"><label htmlFor="name">{locale === "en" ? "Name" : "Nom"}</label><input id="name" name="name" required /></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" required /></div></div><div className="field"><label htmlFor="subject">{locale === "en" ? "Subject" : "Sujet"}</label><input id="subject" name="subject" /></div><div className="field"><label htmlFor="message">{locale === "en" ? "How can we help?" : "Comment pouvons-nous vous aider?"}</label><textarea id="message" name="message" required /></div><label style={{ display: "block", fontSize: ".9rem", lineHeight: 1.5 }}><input name="consent" type="checkbox" required /> {locale === "en" ? "I consent to QBBE using this information to respond to my inquiry." : "Je consens à ce que QBBE utilise ces renseignements pour répondre à ma demande."}</label><button className="button" disabled={state === "sending"} style={{ marginTop: "1.2rem" }}>{state === "sending" ? "…" : locale === "en" ? "Send message" : "Envoyer le message"}</button>{state === "error" && <p className="error" role="alert">{locale === "en" ? "We could not send your message. Please try again." : "Nous n’avons pas pu envoyer votre message. Veuillez réessayer."}</p>}</form>; }
