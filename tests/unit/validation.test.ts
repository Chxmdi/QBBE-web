import { describe, expect, it } from "vitest";
import { donationSchema, newsletterSchema } from "@/lib/validation";
import { checkoutEnabled } from "@/lib/payment-gates";
describe("validation", () => { it("accepts a valid newsletter subscription", () => expect(newsletterSchema.safeParse({ email: "hello@example.org", locale: "en" }).success).toBe(true)); it("rejects an invalid donation amount", () => expect(donationSchema.safeParse({ amount: 0, frequency: "once", locale: "en", donor: { name: "A", email: "a@example.org", designation: "general" } }).success).toBe(false)); it("allows only the current general donation designation", () => { expect(donationSchema.safeParse({ amount: 25, frequency: "once", locale: "en", donor: { name: "A", email: "a@example.org", designation: "general" } }).success).toBe(true); expect(donationSchema.safeParse({ amount: 25, frequency: "once", locale: "en", donor: { name: "A", email: "a@example.org", designation: "academic" } }).success).toBe(false); }); it("requires an explicit QBBE launch flag for each checkout", () => { expect(checkoutEnabled("donation", {})).toBe(false); expect(checkoutEnabled("membership", {})).toBe(false); expect(checkoutEnabled("donation", { QBBE_DONATIONS_ENABLED: "true" })).toBe(true); expect(checkoutEnabled("membership", { QBBE_MEMBERSHIPS_ENABLED: "true" })).toBe(true); }); });
